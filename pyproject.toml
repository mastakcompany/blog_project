[project]
name = "blog-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=6.0.5",
    "loguru>=0.7.3",
    "pre-commit>=4.6.0",
]

[dependency-groups]
dev = [
    "pre-commit>=4.6.0",
]
